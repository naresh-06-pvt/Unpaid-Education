apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: quiz-ingress
spec:
  rules:
  - host: quiz.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: quiz-service
            port:
              number: 80
